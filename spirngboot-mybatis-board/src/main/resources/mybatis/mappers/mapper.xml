<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.example.mybatis.mapper.Mappers">

    <select id="getList" resultType="com.example.mybatis.dao.Question">
        SELECT ID, CONTENT, SUBJECT, CREATE_DATE
        FROM QUESTION
    </select>

    <select id="findById" resultType="com.example.mybatis.dao.Question">
        SELECT ID, CONTENT
        FROM QUESTION
        WHERE ID = #{id}
    </select>

    <select id="getAnswer" resultType="com.example.mybatis.dao.Answer">
        SELECT ID, CONTENT, CREATE_DATE, QUESTION_ID
        FROM ANSWER
        WHERE QUESTION_ID = #{id}
    </select>

    <select id="getQuestion" resultType="com.example.mybatis.dao.Question">
        SELECT ID, CONTENT, CREATE_DATE, SUBJECT
        FROM QUESTION
        WHERE ID = #{id}
    </select>

</mapper>